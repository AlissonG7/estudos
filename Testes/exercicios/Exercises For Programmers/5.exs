#INCOMPLETO
first_input = IO.gets "What is the first number? "
second_input = IO.gets "What is the second number? "

unbreak = fn x -> String.replace_suffix(x, "\n", "")

a = String.to_integer(Unbreaker.string(first_input))
b = String.to_integer(Unbreaker.string(second_input))
IO.puts("#{a} + #{b} = #{a+b}\n#{a} - #{b} = #{a-b}\n#{a} * #{b} = #{a*b}\n#{a} / #{b} = #{a/b}")


String.split(""Date.to_string(Date.utc_today)

Date.new(year, month, day, calendar)
